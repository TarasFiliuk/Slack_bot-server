# Required metadata
sonar.projectKey=forcelate-infrastructure-server
sonar.projectName=Forcelate: Infrastructure Server
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src/main/java

# Comma-separated paths to directories with tests
sonar.tests=src/test/java

# Comma-separated paths to directories with classes binaries
sonar.java.binaries=target/classes

# Tells SonarQube where the unit tests execution reports are
sonar.junit.reportsPath=target/surefire-reports

# Tells SonarQube where the unit tests code coverage report is
sonar.jacoco.reportPaths=target/jacoco.exec

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# Exclude
sonar.exclusions=src/main/java/com/forcelate/infrastructure/Application.java,\
  src/main/java/com/forcelate/infrastructure/configuration/**/*,\
  src/main/java/com/forcelate/infrastructure/domain/**/*,\
  src/main/java/com/forcelate/infrastructure/properties/**/*,\
  src/main/java/com/forcelate/infrastructure/resource/InfoResource.java
